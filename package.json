{
  "name": "do-kubernetes-web-app",
  "version": "1.0.0",
  "description": "Scalable web application for DigitalOcean Kubernetes demonstration",
  "main": "index.html",
  "scripts": {
    "start": "nginx -g 'daemon off;'",
    "build": "echo 'Building static web app...'",
    "test": "echo 'Running tests...'",
    "docker:build": "docker build -t do-web-app .",
    "docker:run": "docker run -p 8080:8080 do-web-app",
    "k8s:deploy": "kubectl apply -f k8s/",
    "k8s:delete": "kubectl delete -f k8s/",
    "load-test": "ab -n 1000 -c 10"
  },
  "keywords": [
    "kubernetes",
    "digitalocean",
    "docker",
    "scaling",
    "web-app",
    "nginx"
  ],
  "author": "Technical Account Manager Candidate",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/do-kubernetes-assignment.git"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "apache-bench": "^1.0.0"
  }
}